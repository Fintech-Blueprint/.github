name: CI\non: [pull_request, push]\n\njobs:\n  ci:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout\n        uses: actions/checkout@v4\n      - name: Lint (placeholder)\n        run: echo "Run linter here"\n      - name: Unit tests (placeholder)\n        run: echo "Run unit tests here"\n      - name: SAST (placeholder)\n        run: echo "Run SAST scanner here"\n      - name: SCA (placeholder)\n        run: echo "Run SCA here"\n      - name: Build Docker image (placeholder)\n        run: echo "Build Docker image here"\n      - name: Generate SBOM (placeholder)\n        run: echo "Generate SBOM here"\n      - name: Sign image (placeholder)\n        run: echo "Sign image here"\n  deploy-staging:\n    if: github.event_name == push && github.ref == refs/heads/main\n    runs-on: ubuntu-latest\n    steps:\n      - name: Deploy to staging (placeholder)\n        run: echo "Trigger GitOps/Argo CD deploy to staging"